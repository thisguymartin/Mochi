# Bug & Issue Backlog

Known issues for MOCHI to address. The entire content of this file is sent
as-is to the AI agent as task context -- the bullet format below is for the
agent to interpret, not parsed by MOCHI.

## Tasks
- Fix null pointer dereference in auth middleware
- Resolve race condition in cache layer
- Fix broken pagination on user list endpoint
